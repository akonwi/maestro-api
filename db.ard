use ard/env
use ard/sqlite

use maestro/bets

fn init() sqlite::Database {
  let db_path = env::get("DB_PATH").or("")
  let db = sqlite::open(db_path).expect("Failed to initialize database")
  bets::init_db(db)
  db
}
